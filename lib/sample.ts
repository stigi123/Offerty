import type { Quote } from "./types";
import { homepageSampleQuote } from "./templates";

/** Fiktive Beispieldaten — keine echten Personen oder Firmen. */
export function sampleQuote(): Quote {
  return homepageSampleQuote();
}
